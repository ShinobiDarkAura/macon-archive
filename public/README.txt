Maçon Archive — drop-in assets
================================

Place these files here (this exact folder, these exact names):

  rebi.png   — Hannah's avatar (the rabbit). Shows on her header + incoming bubbles.
  ori.png    — Alex's avatar (the bear). Shows on your outgoing bubbles.
  bell.mp3   — real sheep-bell notification sound (replaces the synth placeholder).

Notes
-----
- Square images work best (they're masked into a circle). ~128×128px is plenty.
- PNG with transparency is fine. If you'd rather use .jpg, tell me and I'll switch the paths.
- Until rebi.png / ori.png exist, avatars fall back to a plain warm circle (not broken).
- Until bell.mp3 exists, the notification uses the in-browser synth bell automatically.
- To swap which keeper is the rabbit vs the bear, edit the ANIMALS map in index.html.
